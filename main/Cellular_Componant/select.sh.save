for file in `ls -r level_4_goterm_genelist/*gene.tsv`
do
echo $file 
#if (( $(<"${file}" wc -l) > 25 ));theif (( $(<"${file}" wc -l) > 20 ));then
cp -r $file selected_level_4_goterm/
fi
done
#if (( $(<"level_4_goterm_genelist/${file}" wc -l) > 25 ))
